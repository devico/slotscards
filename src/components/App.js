import React, { Component } from 'react';
import Page from './Page';

export default class App extends Component {
  constructor(props) {
    super(props);
  }

  render() {
    return (
      <Page />
    );
  }
}
